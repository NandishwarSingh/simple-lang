// Benchmark: build 1M strings by repeated concatenation — allocation pressure.
fn main() {
    let mut total = 0;
    for (i in 0..1000000) {
        let mut s = "";
        for (j in 0..8) {
            s = s + "abcdefg";
        }
        total = total + len(s);
    }
    print(total);
}
