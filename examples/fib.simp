// Recursive Fibonacci — functions, recursion, if/else.
fn fib(n: int) -> int {
    if (n < 2) {
        return n;
    }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    let mut i = 0;
    while (i <= 10) {
        print(fib(i));
        i = i + 1;
    }
}
